<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/track_item_padding"
    android:background="?attr/colorSurface">

    <ImageView
        android:id="@+id/trackArtwork"
        android:layout_width="@dimen/track_artwork_size"
        android:layout_height="@dimen/track_artwork_size"
        android:scaleType="centerCrop"
        android:contentDescription="@string/track_artwork"
        android:layout_marginEnd="@dimen/track_artwork_margin_end"
        android:background="@drawable/track_placeholder" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/trackName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="@dimen/track_name_text_size"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end"
            android:fontFamily="@font/ys_text_medium"
            android:layout_marginBottom="@dimen/track_name_margin_bottom" />

        <TextView
            android:id="@+id/artistName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/track_artist_text_size"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end"
            android:fontFamily="@font/ys_text_regular" />
    </LinearLayout>

    <TextView
        android:id="@+id/trackTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/track_time_text_size"
        android:textColor="?attr/colorOnSurface"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/track_time_margin_start"
        android:fontFamily="@font/ys_text_regular" />

</LinearLayout>
